const config = {
  // 启动端口
  port: 8899,

  // 数据库配置
  database: {
    DATABASE: 'mydb',
    USERNAME: 'root',
    PASSWORD: 'root',
    PORT: '3306',
    HOST: '101.37.245.134'
  }
}

module.exports = config
